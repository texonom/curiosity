import time

import fire
import chromadb


def upload_embedding(upload: bool = false, db: str = 'chroma') -> str:

def upload_markdown(upload: bool = false) -> str:

if __name__ == '__main__':
  fire.Fire(index)
